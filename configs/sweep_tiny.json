{
  "description": "Short validation sweep for choosing a learning rate and dropout on a tiny prepared dataset.",
  "fixed_training": {
    "max_steps": 300,
    "warmup_steps": 30,
    "eval_interval": 100,
    "eval_batches": 10,
    "save_interval": 300,
    "log_interval": 25,
    "save_numbered_checkpoints": false
  },
  "fixed_model": {},
  "grid": {
    "training.learning_rate": [0.0001, 0.0003, 0.0006],
    "training.weight_decay": [0.01, 0.1],
    "model.dropout": [0.0, 0.1]
  }
}

